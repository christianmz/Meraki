<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/nav_gallery">

    <fragment
        android:id="@+id/nav_gallery"
        android:name="com.meazza.meraki.ui.gallery.GalleryFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_gallery" />
    <fragment
        android:id="@+id/nav_settings"
        android:name="com.meazza.meraki.ui.settings.SettingsFragment"
        android:label="@string/settings"
        tools:layout="@xml/root_preferences" />
    <fragment
        android:id="@+id/nav_fav"
        android:name="com.meazza.meraki.ui.fav.FavFragment"
        android:label="@string/favorites"
        tools:layout="@layout/fragment_fav" />
</navigation>